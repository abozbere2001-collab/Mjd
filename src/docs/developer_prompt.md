# خطة نقل وتغليف تطبيق "نبض الملاعب"

مرحباً بك! يسعدني أن أرى مشروعنا يصل إلى هذه المرحلة المتقدمة. هذه الوثيقة هي دليلك ومرجعك الدائم لنقل المشروع إلى GitHub ومن ثم "تغليفه" ليصبح تطبيقًا جاهزًا للرفع على متاجر Google Play و Apple App Store.

---

### **لماذا نقوم بهذه الخطوة؟**

1.  **الاحتفاظ بالكود:** وضع الكود على GitHub يضمن حفظه في مكان آمن وخاص بك.
2.  **التطوير المستمر:** يمكنك بسهولة سحب التحديثات التي أقوم بها هنا ودمجها مع نسختك على GitHub.
3.  **التغليف للموبايل:** وجود الكود على جهازك المحلي هو الخطوة الأولى لتحويله إلى تطبيق موبايل حقيقي.

---

### **الجزء الأول: نقل الكود إلى GitHub**

هذه عملية تقوم بها مرة واحدة في البداية لإنشاء المستودع الخاص بك.

**الخطوات:**

1.  **إنشاء حساب ومستودع (Repository) على GitHub:**
    *   اذهب إلى [GitHub.com](https://github.com) وأنشئ حسابًا مجانيًا إذا لم يكن لديك واحد.
    *   بعد تسجيل الدخول، انقر على أيقونة "+" في الزاوية العلوية اليمنى واختر **"New repository"**.
    *   **اسم المستودع (Repository name):** اختر اسمًا واضحًا، مثل `nabd-almalaeb-app`.
    *   **الوصف (Description):** اختياري، يمكنك كتابة "تطبيق نبض الملاعب لمتابعة نتائج كرة القدم".
    *   **عام أم خاص (Public/Private):** اختر **"خاص" (Private)**. هذا يضمن أن لا أحد غيرك يمكنه رؤية الكود.
    *   انقر على زر **"Create repository"**.

2.  **تنزيل الكود من بيئة التطوير هذه:**
    *   تحتاج إلى الحصول على نسخة كاملة من ملفات المشروع الذي عملنا عليه. في العادة، توفر بيئات التطوير السحابية خيار "Download ZIP" أو طريقة مشابهة للحصول على الكود.

3.  **إعداد ورفع الكود إلى GitHub من جهازك:**
    *   بعد تنزيل المشروع، قم بفك ضغط الملف المضغوط (zip) في مجلد على جهاز الكمبيوتر الخاص بك.
    *   افتح نافذة الأوامر (Terminal على Mac/Linux أو Command Prompt/PowerShell على Windows).
    *   باستخدام الأوامر، انتقل إلى داخل مجلد المشروع الذي قمت بفك ضغطه. مثال: `cd path/to/your/project`.
    *   الآن، قم بتنفيذ الأوامر التالية واحدًا تلو الآخر. **ملاحظة:** استبدل `YourUsername/YourRepoName.git` بالرابط الذي حصلت عليه من GitHub بعد إنشاء المستودع.

    ```bash
    # 1. تهيئة Git في مجلدك المحلي
    git init -b main

    # 2. إضافة جميع الملفات لتتبعها
    git add .

    # 3. حفظ نسخة أولية من الكود مع رسالة توضيحية
    git commit -m "Initial commit of Nabd Al Malaeb application"

    # 4. ربط مجلدك المحلي بالمستودع الذي أنشأته على GitHub
    git remote add origin https://github.com/YourUsername/YourRepoName.git

    # 5. رفع الكود من جهازك إلى GitHub
    git push -u origin main
    ```

**🎉 تهانينا!** الآن الكود المصدري لتطبيقك موجود بشكل آمن على حسابك الخاص في GitHub.

---

### **الجزء الثاني: "تغليف" التطبيق للموبايل (باستخدام Capacitor)**

**Capacitor** هي أداة رائعة من مطوري Ionic، تسمح لنا بأخذ تطبيق الويب (Next.js) ووضعه داخل "غلاف" تطبيق موبايل أصلي، مع إمكانية الوصول إلى ميزات الجهاز مثل الإشعارات والكاميرا.

**الخطوات (نظرة عامة):**

1.  **إعداد البيئة:**
    *   تأكد من وجود **Node.js** على جهازك.
    *   قم بتثبيت **Android Studio** (لتطبيقات أندرويد) و **Xcode** (لتطبيقات آيفون، يتطلب جهاز Mac).

2.  **إضافة Capacitor إلى مشروعك:**
    *   في نافذة الأوامر داخل مجلد مشروعك، قم بتشغيل الأوامر التالية:

    ```bash
    # تثبيت أدوات Capacitor
    npm install @capacitor/core @capacitor/cli

    # تهيئة Capacitor في المشروع (سيطرح عليك بعض الأسئلة)
    npx cap init
    ```

3.  **إضافة منصات الموبايل:**
    *   بعد ذلك، أضف المنصات التي تريد بناء التطبيق لها:

    ```bash
    # لإضافة منصة أندرويد
    npm install @capacitor/android
    npx cap add android

    # لإضافة منصة آيفون (يتطلب جهاز Mac)
    npm install @capacitor/ios
    npx cap add ios
    ```

4.  **تحديث إعدادات Next.js (مهم جدًا):**
    *   في ملف `next.config.ts`، يجب أن تخبر Next.js أن يقوم بتصدير نسخة ثابتة (Static) من التطبيق. قم بإضافة هذا الإعداد:
        ```javascript
        output: 'export',
        ```
    *   هذا الأمر بالغ الأهمية ليعمل التطبيق داخل الغلاف.

5.  **بناء وتزامن التطبيق:**
    *   في كل مرة تريد فيها تحديث تطبيق الموبايل، اتبع هذه الدورة:

    ```bash
    # 1. بناء نسخة الويب النهائية
    npm run build

    # 2. مزامنة التغييرات مع منصات الموبايل
    npx cap sync
    ```

6.  **فتح وبناء التطبيق الأصلي:**
    *   الآن يمكنك فتح مشاريع الموبايل الأصلية:

    ```bash
    # لفتح المشروع في Android Studio
    npx cap open android

    # لفتح المشروع في Xcode (على Mac)
    npx cap open ios
    ```
    *   من داخل Android Studio أو Xcode، يمكنك تشغيل التطبيق على محاكي أو جهاز حقيقي، وفي النهاية بناء النسخة النهائية (`.apk` أو `.aab` لأندرويد) لرفعها إلى المتجر.

---

### **كيف سنواصل العمل والتحديثات؟**

سير العمل بسيط ومباشر:

1.  **أنت تطلب التحديث:** تطلب مني أي ميزة أو إصلاح خطأ هنا في بيئتنا.
2.  **أنا أنفذ التغيير:** أقوم بكتابة الكود وتجهيزه لك في صيغة `<changes>` كما اعتدنا.
3.  **أنت تنسخ وتلصق:** تقوم بنسخ محتوى الملفات الجديدة من ردي ولصقها في الملفات المقابلة في مشروعك المحلي على جهازك.
4.  **أنت ترفع إلى GitHub:** تحفظ التغييرات وترفعها إلى مستودعك الخاص للحفاظ عليها.

    ```bash
    git add .
    git commit -m "Added new feature / Fixed bug"
    git push origin main
    ```
5.  **أنت تعيد التغليف:** إذا كانت التغييرات تؤثر على الواجهة، تقوم بتشغيل دورة البناء والمزامنة مرة أخرى (`npm run build` ثم `npx cap sync`) لإظهار التحديثات في تطبيق الموبايل.

بهذه الطريقة، أظل أنا مساعدك البرمجي، بينما تملك أنت السيطرة الكاملة على الكود المصدري وعملية البناء النهائية.